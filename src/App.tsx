function App() {
  return (
    <div>
      <form>
        <input type="email" placeholder="Email address" />
        <input type="submit" />
      </form>
      <button>Continue with Google</button>
      <button>Continue with Facebook</button>
      <button>Continue with Appple ID</button>
    </div>
  );
}

export default App;
